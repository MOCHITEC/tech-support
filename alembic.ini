# Alembic config. Schema-changing agent fixes also live under app/migrations/
# (inside the bot's allowed path). The connection URL is resolved in env.py
# via the DATABASE_URL logic.
[alembic]
script_location = app/migrations
