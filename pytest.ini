[pytest]
# repo ルートを import パスに追加(console script の `pytest` は cwd を
# sys.path に入れないため、CI で `from app...` が失敗するのを防ぐ)。
pythonpath = .
markers =
    integration: needs a real PostgreSQL (set TEST_DATABASE_URL); skipped otherwise
filterwarnings =
    ignore:Using .httpx. with .starlette.testclient.*
